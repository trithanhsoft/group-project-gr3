import SettingsManagement from "@/modules/admin/settings/SettingsManagement";

export const metadata = { title: "Cài đặt hệ thống | Admin" };

export default function SettingsPage() {
  return <SettingsManagement />;
}
